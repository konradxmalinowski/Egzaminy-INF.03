-- 1
SELECT MIN(priorytet) FROM notatki WHERE Osoby_id = 3;

-- 2
SELECT nazwa, priorytet FROM notatki WHERE nazwa like '%na%';

-- 3
SELECT osoby.imie, notatki.nazwa FROM notatki JOIN osoby ON notatki.Osoby_id = osoby.id WHERE notatki.priorytet = 5;

-- 4
SELECT osoby.imie, count(notatki.id) FROM osoby JOIN notatki ON notatki.Osoby_id = osoby.id GROUP BY osoby.imie;