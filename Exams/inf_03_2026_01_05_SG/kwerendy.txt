-- 1
SELECT id, imie, nazwisko FROM personel WHERE status = 'policjant';

-- 2
SELECT typ, COUNT(id) FROM pojazdy GROUP BY typ;

-- 3
SELECT personel.id, personel.nazwisko FROM personel LEFT JOIN rejestr ON rejestr.id_personel = personel.id WHERE rejestr.id IS NULL;

-- 4
INSERT INTO rejestr (id_personel, id_pojazd, data) VALUES (1, 14, CURRENT_DATE());